============================================================
 世界杯/苏超赛事信息与互动预测平台
 README
============================================================

1. GitHub 仓库地址
------------------------------------------------------------
https://github.com/Luzhongyuan251250128/Homework_football_platform
（公开仓库，无需访问授权）
（本项目完整源码、数据库初始化脚本与 API 文档均在仓库中）

2. Docker Image / Docker Compose 启动命令
------------------------------------------------------------
环境要求：X64 架构的 Linux 主机（或装有 Docker Desktop 的 Windows/Mac），已安装 Docker 与 Docker Compose。

方式一（推荐，一条命令启动完整平台）：
进入项目根目录（含 docker-compose.yml），执行：

    docker compose up -d --build

等待镜像构建与数据库初始化完成后，访问：

    前端页面：http://localhost        （或服务器公网 IP）
    后端 API ：http://localhost:3000

方式二（直接使用交付的镜像产物包，无需构建）：
将随作业提交的镜像包导入：

    docker load -i football-backend-image.tar.gz
    docker load -i football-frontend-image.tar.gz

导入后用容器运行即可（数据库需按下方第 3 节配置连接，或直接使用方式一）。

其他常用命令：

    docker compose ps                    查看运行状态
    docker compose down                  停止服务
    docker compose down -v && docker compose up -d --build   重置演示数据

3. 数据库与资源文件挂载说明及配置方法
------------------------------------------------------------
(1) 数据库初始化
    容器 mysql 首次启动时，自动执行挂载的初始化脚本：
        ./db/init.sql → /docker-entrypoint-initdb.d/init.sql
    脚本会创建数据库 football_db、8 张业务表并导入全部种子数据：
    - 30 场比赛（世界杯小组赛 18 场 + 苏超第 1~2 轮 12 场，含预置比分）
    - 15 个账号（admin + 10 个演示账号 + 4 个球迷账号）
    - 100 条预测记录、35 条帖子、69 条评论（全部比赛均有预测与讨论内容）
    - 31 支球队档案（含预测相关话术评价）与 37 条球队评价区评论
    种子数据详细说明见 db/seed-data.md。

(2) 数据持久化
    使用命名卷 mysql_data 挂载到 /var/lib/mysql，容器删除后数据仍保留；
    如需全新初始化演示数据，执行 docker compose down -v 后重启。

(3) 数据库配置
    连接配置在 docker-compose.yml 的 mysql 服务环境变量中：
        MYSQL_ROOT_PASSWORD / MYSQL_DATABASE / MYSQL_USER / MYSQL_PASSWORD
    后端通过环境变量连接数据库（backend 服务）：
        DB_HOST=mysql  DB_PORT=3306  DB_USER=football  DB_PASSWORD=football123  DB_NAME=football_db
    如需修改，改 docker-compose.yml 后 docker compose up -d 重启即可。

(4) 资源文件
    前端为构建产物（nginx 静态托管），无需额外挂载；若需持久化上传类资源，
    可在 frontend 服务中自行挂载卷（本项目未使用文件上传）。

4. Web 服务公网访问地址
------------------------------------------------------------
本项目未部署公网环境，验收请使用本地演示：

    http://localhost

（也可按第 2 节方式将平台部署到任意公网服务器后，访问该服务器的公网 IP。）

5. 演示流程（核心演示点：虚拟时间）
------------------------------------------------------------
本项目所有比赛时间均为"虚拟时间"，演示者可通过管理员时间控制台手动推进
时间，完整演示"未开赛 → 进行中 → 已完赛"的比赛生命周期：
预测 → 比分刷新 → 积分结算 → 讨论区开放。

演示步骤：
  (1) 打开首页，页面上方可看到"得分要求"提示框（猜中准确比分 +3 分、
      猜中胜平负方向 +1 分、猜错 +0 分）；按"未开赛 / 进行中 / 已完赛"
      三组展示的 30 场比赛，右上角显示当前虚拟时间：2026-07-10 12:00:00；
  (2) 注册新用户（或用演示账号登录，演示账号密码均为 123456，用户名以"小"字开头，
      如：小射手），对一场"未开赛"的比赛提交比分预测；
  (3) 退出登录，用 admin / admin123 登录，点击右上角"时间控制台"，
      按 +15 分钟 / +1 小时推进虚拟时间（或直接设定时间）；
  (4) 回到首页：进行中的比赛显示实时比分（LIVE），随时间推进变为"已完赛"，
      比分刷新展示；积分自动结算（猜中比分 +3 分、猜中胜负平 +1 分）；
      每场比赛均已预置球迷帖子与评论，完赛后讨论区即可查看与参与；
      首页的"世界杯 / 苏超"筛选在进入比赛详情后返回时保持不变；
  (5) 登录普通用户，进入比赛详情页查看预测结算得分，参与讨论区发帖与评论
      （评论支持回复他人评论）；点击记分牌上的队名进入"球队主页"，
      查看该队战绩、专属档案评价、你对该队的历史预测与准确次数、球队评价区；
  (6) 打开"排行榜"页查看积分名次（含已出结果预测数，结算后实时更新），
      点击任一用户名可查看该用户的过往预测与未来预测；个人中心查看
      预测记录（含每场 +0/+1/+3 得分）与累计积分。

6. 可选：课程中印象最深刻的内容
------------------------------------------------------------
下午的实践课。没有下午课的实操展示，光靠上午的讲解真的学不懂。

7. 可选：对课程的改进建议
------------------------------------------------------------
建议 PPT 写得更详细一些。单纯看 PPT 感觉被知识轰炸——内容密度大，
而且大部分内容之前没有了解，需要边学边现查。

============================================================
 技术栈：Vue 3 + Vite | Node.js + Express | MySQL | Docker Compose
 API 文档：docs/api.md   数据库脚本：db/init.sql   种子数据说明：db/seed-data.md
============================================================
